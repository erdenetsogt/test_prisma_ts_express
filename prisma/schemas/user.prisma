model User {
  id           Int      @id @default(autoincrement())
  email        String?
  password     String?
  refreshToken String?
  status       Int?
  peopleId     Int?
  companyId    Int?
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt

  @@map("users")
}