model PeopleDoctor {
  id        Int      @id @default(autoincrement())
  peopleId  Int?
  degree    String?
  place     String?
  date      String?
  cerf      String?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@map("peopleDoctors")
}