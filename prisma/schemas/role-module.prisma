model RoleModule {
  id              Int      @id @default(autoincrement())
  moduleCompanyId Int
  roleId          Int
  read            Boolean
  update          Boolean
  write           Boolean
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt

  @@map("roleModules")
}