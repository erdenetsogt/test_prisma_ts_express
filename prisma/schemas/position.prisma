model Position {
  id           Int      @id @default(autoincrement())
  name         String?
  departmentId Int?
  status       Int?
  startdate    DateTime
  enddate      DateTime?

  @@map("positions")
}