model People {
  id          Int      @id @default(autoincrement())
  firstName   String
  lastName    String
  register    String
  birthday    String
  birthcityId Int
  genderId    Int
  ovog        String
  nationalId  Int
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}