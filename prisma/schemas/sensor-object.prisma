// ... existing Prisma schema ...

model SensorObject {
  id          Int      @id @default(autoincrement())
  name        String
  sensorId    Int
  description String?
  companyId   Int
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  @@map("sensorObjects")
}

// ... rest of the Prisma schema ...