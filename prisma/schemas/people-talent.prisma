model PeopleTalent {
  id        Int      @id @default(autoincrement())
  peopleId  Int
  talentId  Int
  vote      Int?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@map("peopleTalents")
}